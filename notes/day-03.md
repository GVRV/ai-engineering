Loss is a single scalar that scores the whole batch. Backward on that scalar fills .grad on every parameter.

parameter count for MLP(3, [4,4,1]): 41
starting loss: 8.009592342135461
ending loss (step 100): 0.007546253783483685
the bug I hit: Nothing major, I hit a missing `__rsub__` bug during playing around with the REPL so I added that to `Value`.
why zero-grad: During backpropogation, the gradients in our parameters are added up cumulatively because as we discussed in Day 2, we want the cumulative effect of different cross product rules to get the final gradient for a single parameter. However, on subsequent backward passes, we want to re-calculate the gradient of each parameter from 0 because as we have nudged their values in the meanwhile to minimise the loss and their new value should involve calculating the gradient from scratch. Retaining their old gradient value and then adding to it cumulatively during a subsequent backward pass will give us the wrong value for the gradient leading to incorrect network optimization.

what I still don't trust: How to correctly estimate training loop numbers and the "nudge" size (though Andrej mentioned decaying it which is interesting). I'm not clear on the math around tanh/ReLU (i.e why pick tanh over another function that can squish between 0 and 1 or why pick ReLU over some function that returns only 1 if the number is positive etc). I understand what these functions are but I don't understand why they and only they are used for squishing/activation, what makes them special instead of possibly other functions. The same can be said of loss calculation function: I understand squared mean loss but why that function over something else? I still don't understand much of PyTorch since I haven't played around with it at all (i.e. writing a full training loop in PyTorch) but I guess we will do that in the future.